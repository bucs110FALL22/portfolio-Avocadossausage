def isinscreen(window, turt):
  turtleX = t.xcor()
  turtleY = t.ycor()

  x_range = wn.window_width()/2
  y_range = wn.window_height()/2
  if abs(turtleX) > x_range or abs(turtleY) > y_range:
    return False
  return True

distance = 10
angle = 90
is_in_screen = True
## pop project hint for display animations (cycles through lists)
